class Solution {
public:
    void reverseString(vector<char>& s) {
        // int n=s.size();
        // int index=0;
        // for(int i=n-1;i>0;i--){
        //     s[index++]=s[i];
        //   }
        reverse(s.begin(),s.end());
        
      
    }
};